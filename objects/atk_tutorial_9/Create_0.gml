


// Inherit the parent event
event_inherited();
soul.mode = 0;
MAX_SHOTS = 8 + global.difficulty * 4

MAX_TRAIL_BULLETS = floor(sqrt(global.difficulty) * 16)
MAX_LARGE_BULLETS = (15 + global.difficulty) * sign(global.difficulty)
MAX_BULLETS = 24 + global.difficulty * 8

shotsFired = 0
bb.boxScaleDest = new Vector2(room_height/3, room_height/4);
