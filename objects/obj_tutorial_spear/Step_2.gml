


// Inherit the parent event
event_inherited();

image_angle = point_direction(0, 0, velocity.x, velocity.y)

if (reflected && (x < -128 || room_width + 128 < x || y < -128 || room_height + 128 < y))
{
	instance_destroy();
}