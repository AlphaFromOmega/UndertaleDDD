


// Inherit the parent event
event_inherited();
soul.mode = 0;
MAX_SHOTS = ((global.difficulty + 1) * 10) + 1
MAX_BULLETS = 180
bb.boxScaleDest = new Vector2(room_height/3, room_height/4);
shots_fired = 0;

for (var i = 0; i < MAX_SHOTS; i++;)
{
	hitBox[i] = false;
}
lowest_bullet = noone;
